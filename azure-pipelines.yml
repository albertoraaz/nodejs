trigger:
- main
pool:
  vmImage: 'ubuntu-18.04'
  variables:
  imageName: 'demo.goharbor.io/nodejs:$(build.buildId)'
steps:
- task: Docker@2
  displayName: Build an image
  inputs:
    repository: $(imageName)
    command: build
    Dockerfile: Dockerfile
    