trigger:
- main
pool:
  vmImage: 'ubuntu-18.04'
  variables:
  imageName: 'nodejs'
steps:
- task: Docker@2
  displayName: Login to Harbor
  inputs:
    command: login
    containerRegistry: hubrepo3
- task: Docker@2
  displayName: Build an image
  inputs:
    command: build
    repository: nodejs/repository
    containerRegistry: hubrepo3
    Dockerfile: Dockerfile
    tags: $(build.buildId)
- script: docker login https://index.docker.io/v1/ --username=albertoraaz --password=ven320wl8ven
- script: docker image ls